{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/common/DeletedEntry.schema.json",
  "title": "DeletedEntry",
  "description": "Generic wrapper for a single soft-deleted record. value holds the original payload untouched and can be any JSON value — it differs per section. deletion holds the metadata. Container kind is always preserved under value: a deleted element from a string[] attribute has value: [\"the-string\"] (single-element array), not a bare string. This wrapper covers object records, string lists, and bare scalar fields uniformly.",
  "type": "object",
  "required": ["value", "deletion"],
  "properties": {
    "value": {},
    "deletion": {
      "$ref": "https://schemas.certifyos.com/common/Deletion.schema.json"
    }
  },
  "additionalProperties": false
}
