{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/enums/PlanStatus.schema.json",
  "title": "Plan Status Enum",
  "description": "Specifies the status of a healthcare plan.",
  "type": "string",
  "enum": [
    "Active",
    "Terminated",
    "Pending",
    "Inactive"
  ]
} 