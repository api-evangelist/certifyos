{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/enums/NetworkPlanStatus.schema.json",
  "title": "Network Plan Status Enum",
  "description": "Specifies the status of a plan's relationship with a network.",
  "type": "string",
  "enum": [
    "Active",
    "Terminated"
  ]
} 