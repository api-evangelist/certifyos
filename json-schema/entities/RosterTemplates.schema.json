{
  "$schema": "http://json-schema.org/draft/2020-12/schema#",
  "$id": "https://schemas.certifyos.com/entities/RosterTemplates.schema.json",
  "title": "roster_templates Table Schema",
  "description": "Describes the schema for the roster_templates database table.",
  "type": "object",
  "properties": {
    "template_name": {
      "type": "string",
      "maxLength": 255,
      "description": "Name of the roster template."
    },
    "description": {
      "type": "string",
      "description": "Detailed description of the roster template."
    },
    "status": {
      "type": "string",
      "maxLength": 255,
      "description": "Current status of the roster template."
    },
    "match_threshold": {
      "type": "number",
      "format": "float",
      "description": "Threshold for matching roster data."
    },
    "sample_csv_path": {
      "type": "string",
      "description": "Path to a sample CSV file for the template."
    }
  }
}
