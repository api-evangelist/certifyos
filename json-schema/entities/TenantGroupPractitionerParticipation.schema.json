{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/TenantGroupPractitionerParticipation.schema.json",
  "title": "TenantGroupPractitionerParticipation",
  "description": "Represents the participation of a tenant group practitioner in a specific plan.",
  "type": "object",
  "properties": {
    "enrollmentEffectiveDate": {
      "description": "The date when the participation becomes effective.",
      "type": "string",
      "format": "date"
    },
    "enrollmentTermDate": {
      "description": "The date when the participation is terminated.",
      "type": "string",
      "format": "date"
    }
  }
}
