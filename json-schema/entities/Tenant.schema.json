{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/Tenant.schema.json",
  "title": "Tenant",
  "description": "Represents a tenant or organization within the system.",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the tenant or organization.",
      "type": "string"
    },
    "isSuperOrg": {
      "description": "Indicates if this tenant is a super organization.",
      "type": "boolean"
    },
    "website": {
      "description": "The website URL of the tenant or organization.",
      "type": "string",
      "format": "url"
    },
    "status": {
      "$ref": "https://schemas.certifyos.com/enums/Status.schema.json",
      "description": "The current status of the tenant."
    }
  },
  "required": [
    "name",
    "isSuperOrg",
    "status"
  ],
  "additionalProperties": false
}
