{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/CredentialingWorkflow.deletedData.schema.json",
  "title": "CredentialingWorkflow deletedData",
  "description": "Shape of the deleted_data JSON column for the credentialing_workflows Spanner table. Mirrors the data column structure: sections live under practitioner.* — the practitioner object shape is identical to CorePractitioner.deletedData (entities/CorePractitioner.deletedData.schema.json). Each archived element is a DeletedEntry wrapping the original payload.",
  "type": "object",
  "properties": {
    "practitioner": {
      "$ref": "https://schemas.certifyos.com/entities/CorePractitioner.deletedData.schema.json"
    }
  },
  "additionalProperties": false
}
