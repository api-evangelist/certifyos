{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/TenantGroup.schema.json",
  "title": "TenantGroup",
  "description": "Represents the details of the association between a tenant and a group.",
  "type": "object",
  "properties": {
    "contactPerson": {
      "description": "Details of the contact person for the tenant group.",
      "$ref": "https://schemas.certifyos.com/entities/EntityContacts.schema.json"
    },
    "externalGroupId": {
      "description": "Identifier of the group in an external system.",
      "type": "string"
    },
    "active": {
      "description": "Indicates whether the tenant group is currently active.",
      "type": "boolean"
    },
    "npdbEnrollmentEnabled": {
      "description": "Indicates if NPDB (National Practitioner Data Bank) enrollment is enabled for this group.",
      "type": "boolean"
    }
  }
}
