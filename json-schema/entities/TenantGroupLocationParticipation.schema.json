{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/TenantGroupLocationParticipation.schema.json",
  "title": "TenantGroupLocationParticipation",
  "description": "Represents the participation of a plan at a specific tenant group location.",
  "type": "object",
  "properties": {
    "enrollmentEffectiveDate": {
      "description": "The date when the plan's participation at this location became effective for the tenant group.",
      "type": "string",
      "format": "date"
    },
    "enrollmentTermDate": {
      "description": "The date when the plan's participation at this location terminated for the tenant group (if applicable).",
      "type": "string",
      "format": "date"
    }
  },
  "additionalProperties": false
}
