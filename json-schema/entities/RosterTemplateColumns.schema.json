{
  "$schema": "http://json-schema.org/draft/2020-12/schema#",
  "$id": "https://schemas.certifyos.com/entities/RosterTemplateColumns.schema.json",
  "title": "Roster Template Columns Table Schema",
  "description": "Describes the schema for the roster_template_columns database table.",
  "type": "object",
  "properties": {
    "column_name": {
      "type": "string",
      "maxLength": 255,
      "description": "Name of the column."
    },
    "db_field_name": {
      "type": "string",
      "maxLength": 255,
      "description": "Name of the database field."
    },
    "entity_type": {
      "type": "string",
      "maxLength": 255,
      "description": "Type of entity."
    },
    "is_key_identifier": {
      "type": "boolean",
      "description": "Indicates if the column is a key identifier."
    },
    "is_required": {
      "type": "boolean",
      "description": "Indicates if the column is required."
    },
    "name_hash": {
      "type": "string",
      "maxLength": 64,
      "description": "Hash of the column name."
    }
  }
}
