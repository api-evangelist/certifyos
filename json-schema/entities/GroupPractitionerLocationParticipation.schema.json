{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/GroupPractitionerLocationParticipation.schema.json",
  "title": "GroupPractitionerLocationParticipation",
  "description": "Describes the participation of a practitioner at a specific location within a group.",
  "type": "object",
  "properties": {
    "enrollmentEffectiveDate": {
      "type": "string",
      "format": "date",
      "description": "The date when the practitioner's participation at this location became effective for the group"
    },
    "enrollmentTermDate": {
      "type": "string",
      "format": "date",
      "description": "The date when the practitioner's participation at this location terminated for the group (if applicable)"
    }
  },
  "additionalProperties": false
}
