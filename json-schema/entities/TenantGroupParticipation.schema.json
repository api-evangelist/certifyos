{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/TenantGroupParticipation.schema.json",
  "title": "TenantGroupParticipation",
  "description": "Represents the participation of a tenant group in a specific network under a contract.",
  "type": "object",
  "properties": {
    "enrollmentEffectiveDate": {
      "description": "The date when the tenant group's participation in the network becomes effective.",
      "type": "string",
      "format": "date"
    },
    "enrollmentTermDate": {
      "description": "The date when the tenant group's participation in the network is terminated.",
      "type": "string",
      "format": "date"
    }
  }
}
