{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.certifyos.com/entities/TenantGroupSpecialty.schema.json",
  "title": "TenantGroupSpecialty",
  "description": "Represents the specialty information for a healthcare provider group.  References the Specialty entity.",
  "type": "object",
  "properties": {
    "effectiveDate": {
      "description": "The date when the specialty became effective.",
      "type": "string",
      "format": "date"
    },
    "terminationDate": {
      "description": "The date when the specialty was terminated.",
      "type": "string",
      "format": "date"
    },
    "isPrimary": {
      "description": "Indicates if the specialty is primary.",
      "type": "boolean"
    }
  },
  "required": [
    "effectiveDate",
    "terminationDate",
    "isPrimary"
  ],
  "additionalProperties": false
}
